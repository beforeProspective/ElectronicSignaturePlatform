# ElectronicSignaturePlatform
开发一个电子合同签署平台。技术栈要求：前端 Vue3 + PDF.js（用于渲染PDF），后端 Python (FastAPI 或 Django) + SQLite 数据库。前后端必须严格模块化分层。功能要求：用户上传 PDF 格式的合同、在页面上指定需要签名的位置、生成唯一的签署链接发送给签署人、签署人打开链接在画板上绘制签名，系统将签名合成到 PDF 中并提供最终版下载。所有逻辑必须拆解...
